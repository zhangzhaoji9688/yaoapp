# 部署操作清单（2026-09-02 更新，最终版 v4）

> 目的：把本地全部修复（删除 bug + 登录版 + 三项优化 + 日期单点 + 自动填充修复 + 管理员账号管理 + 密码后门 + 重复订单号清理）同步到阿里云线上
> 线上现状：旧版（无登录）+ db.json 数据（172 条）
> 部署后：登录版 + SQLite，**数据以线上现有 db.json 为准，本地数据不推送**

---

## 一、本地已完成（无需你再操作）

| 项 | 状态 |
|---|---|
| 删除 bug 代码修复 | ✅ commit `79d0500`，已 push |
| 三项优化（订单号去重 / 日期容错 / 改密验证旧密码） | ✅ commit `2901c3b`，已 push |
| 日期只允许单个时间点 | ✅ commit `927a24c`，已 push |
| 搜索框防自动填充（Chrome 填 admin 问题，三层防御） | ✅ commit `c861c33` + `51e8da0`，已 push |
| 管理员账号管理（账号列表 + 一键重置密码） | ✅ commit `6bcee76`，已 push |
| **GitHub 同步** | ✅ `origin/main` 与本地完全一致（0 未推送） |

> 数据策略说明（按你的要求）：
> - **线上数据库 = 服务器现有 `/opt/yaoapp/data/db.json`**（172 条，不动它）
> - **本地数据不推送**：本地 `app.db`（含测试录入）仅作本地测试环境用
> - 新版首次启动会自动把 `data/db.json` **迁移进 SQLite**（admin 账号），所以服务器上的 db.json 就是最终数据源

---

## 二、关于 db.json（已按你的决定生成最终版）

新版启动时**自动迁移**服务器上的 `db.json` 到 SQLite。本地已生成**最终部署数据**：

`D:\AI_Workspace\projects\yaoapp\data\deploy\db.json`（154 条）：
- 内容 = 线上现有 172 条（162 个唯一订单号）为基准
- **已删掉你本地测试时删除的 8 条哈德逊记录**（含 DJ-162 `112-1162952-3305852`，正是当初删不掉那条）
- 乱码数组"多��"34 条已合并进多恩、4 条 shop 乱码已修复
- **已清理 10 组历史重复订单号**（共删 10 条，按规则：无乱码优先 → 乱码最少 → 信息最全 → 最早录入）

> ⚠️ **必须上传此文件**，否则服务器迁移的是旧 db.json（172 条），那 8 条（含 DJ-162）会回来。
> 上传后部署完：哈德逊 42 / 辰润 17 / 伯克纳 29 / 阿达姆 8 / 多恩 58 = **154 条**。
> 说明：其余 20 条记录的退货原因里仍有个别乱码字符（U+FFFD，当年导入损坏，非重复数据），不影响功能，可后续单独处理。

---

## 三、服务器终端操作（阿里云控制台 → 实例 → 远程连接）

```bash
# 0) 先停旧服务，再备份（保证备份的是干净的 db.json）
pkill -f "node server.js" 2>/dev/null; sleep 1
cd /opt
cp -r yaoapp yaoapp.bak.$(date +%Y%m%d_%H%M%S)
ls -d yaoapp.bak.*        # 确认备份成功

# 1) 检查 Node 版本（better-sqlite3 需要 Node ≥ 14，推荐 16+）
node -v
# 版本过低先升级 Node，否则 npm install 装不上 better-sqlite3

# 2) 拉取最新代码（线上是旧版，用重装方式）
rm -rf yaoapp
git clone https://github.com/zhangzhaoji9688/yaoapp.git
cd yaoapp

# 3) 放数据：把 data/deploy/db.json 上传到 /opt/yaoapp/data/db.json
#    （控制台「文件管理」上传，或用 scp。这步必做，见第二节说明）
mkdir -p data
# 确认: ls -la data/db.json 存在

# 4) 安装依赖（国内源，better-sqlite3 优先用预编译二进制）
npm config set registry https://registry.npmmirror.com
npm install

# 5) 启动（首次启动自动：建 admin 账号 + 迁移 db.json 进 SQLite）
nohup node server.js > server.log 2>&1 &

# 6) 立即拿 admin 初始密码（只打印这一次！）
sleep 2
grep -A3 "初始管理员" server.log
# 记下用户名 admin 和初始密码

# 7) 验证新版生效
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/me
# 期望 401（未登录）→ 新版生效；旧版会返回 404
```

---

## 四、浏览器验证

1. 打开 `http://39.96.85.123:3000` —— 应出现**登录界面**（旧版没有）
2. 用 `admin` + 刚才记下的初始密码登录
3. 右上角「🔑 改密」把密码改成自己的
4. 检查数据：哈德逊 42 / 辰润 17 / 伯克纳 29 / 阿达姆 8 / 多恩 58 = **154 条**（DJ-162 等 8 条已不在，10 组重复订单号已清理），全部可正常删除
5. 实测：录入重复订单号被拦截、日期输 `6.1-6.5` 被拒绝、删一条记录成功
6. **账号管理**：admin 顶部有「👥 账号管理」按钮 → 能看到全部账号（用户名/注册时间/最后登录/记录数）；组员忘密码时点「重置密码」输入新密码即可（无需旧密码，重置后对方被踢下线需重新登录）

---

## 五、以后更新代码的固定步骤

```bash
cd /opt/yaoapp
git pull origin main
npm install
pkill -f "node server.js"; sleep 1
nohup node server.js > server.log 2>&1 &
```

> 注意：`git pull` **不会覆盖** `data/db.json`（已加入 .gitignore），数据安全。

---

## 常见问题

- **登录框不出来 / /api/me 返回 404**：代码没更新成功，检查第 2 步 clone 是否成功。
- **数据是空的**：db.json 没放对位置。上传覆盖 `data/db.json` 后，删掉 `data/app.db*` 再重启一次（会自动重新迁移）。
- **npm install 报 better-sqlite3 编译错误**：Node 版本太低，先升级 Node（≥ 14）。
- **忘记 admin（或任何人）的密码 → 用密码后门**（最推荐，不丢任何数据）：
  ```bash
  cd /opt/yaoapp
  node reset-password.js --list               # 先看有哪些账号
  node reset-password.js admin 你的新密码      # 重置 admin 密码（同时踢掉已登录会话）
  ```
  说明：密码是 scrypt 加盐哈希，**任何地方都没有明文可看**（这是安全设计，不是缺陷）；后门脚本需登录服务器才能用，不暴露网络接口，比明文安全。**不要用删 app.db 的方式**（会丢全部账号和数据）。
- **组员忘记密码**：用 admin 登录 →「👥 账号管理」→ 该用户「重置密码」→ 输入新密码告知对方即可（管理员无需知道旧密码）。
- **想回滚**：`rm -rf /opt/yaoapp && mv /opt/yaoapp.bak.<日期> /opt/yaoapp`，旧版即恢复。
