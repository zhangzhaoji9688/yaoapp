services:
  yaoapp:
    image: node:20-alpine
    container_name: yaoapp
    working_dir: /app
    ports:
      - '3000:3000'
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    command: sh -c 'npm install -g json-server && npx json-server --host 0.0.0.0 --port 3000 data/db.json'
